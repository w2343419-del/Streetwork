/**
 * @brief 矩阵中的循环移动模拟
 * 
 * 问题描述：
 * N×M矩阵，每个位置有方向指示(U/D/L/R)
 * 从给定位置出发，按方向移动step次，求最终位置
 * 
 * 算法分析：
 * - 方法1：直接模拟step次移动
 * - 方法2：检测循环，利用循环节优化
 * - 边界处理：到达边界时循环到对侧
 * - 时间复杂度：O(step) 或 O(N×M) 使用循环检测
 * - 空间复杂度：O(1)
 * 
 * 优化思路：
 * 检测循环路径，如果进入循环则利用模运算快速计算
 */
3 5 3000

输出
5 1
2 4
3 3
4 1
3 2

解释
对于第 1 次询问，(1,1) 向上移动一次到达 (5,1)
对于第 2 次询问， 向左移动一次到达 (2,5)，向左移动一次到达 (2,4)
对于第 3 次询问，路径为 (3,3),(2,3),(3,3),(2,3),(3,3),(2,3),(3,3),(2,3),(3,3),(2,3),(3,3)*/

#include <stdio.h>
int main() {
    int N, M;
    scanf("%d %d", &N, &M);

    char S[51][51];
    for (int i = 0; i < N; i++) {
        scanf("%s")
    }
}