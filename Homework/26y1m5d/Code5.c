/**
 * @brief 中序固定二叉树的最大加分DP
 * 
 * 问题描述：
 * 已知二叉树的中序遍历固定为1..n，每个节点i有分数di
 * 子树加分 = 左子树加分 × 右子树加分 + 根分数
 * 空子树加分为1。求最大总加分及对应的前序遍历
 * 
 * 算法分析：
 * - 区间DP：
 *   dp[l][r] 表示区间[l,r]作为子树的最大加分
 *   root[l][r] 记录达到最大加分的根节点位置
 * - 状态转移：
 *   dp[l][r] = max_{k∈[l,r]}(dp[l][k-1] * dp[k+1][r] + d[k])
 *   边界：l>r时加分为1（空子树），l==r时加分为d[l]
 * - 结果：dp[1][n]为最大加分，依据root数组递归输出前序
 * 
 * 时间复杂度：O(n³) - 三重循环区间转移，n<30可接受
 * 空间复杂度：O(n²)
 * 
 * 优化点：
 * - 记录根数组以重建最优前序
 * - 使用long long存储避免溢出
 */
/*设一个 n 个节点的二叉树 tree 的中序遍历为(1,2,3,…,n)，其中数字 1,2,3,…,n 为节点编号。
每个节点都有一个分数（均为正整数），记第 i 个节点的分数为 di，tree 及它的每个子树都有一个加分，
任一棵子树 subtree（也包含 tree 本身）的加分计算方法如下：
subtree 的左子树的加分 × subtree 的右子树的加分 + subtree 的根的分数。
若某个子树为空，规定其加分为 1，叶子的加分就是叶节点本身的分数。不考虑它的空子树。
试求一棵符合中序遍历为 (1,2,3,…,n) 且加分最高的二叉树 tree。要求输出
tree 的最高加分。
tree 的前序遍历。

输入格式
第 1 行 1 个整数 n，为节点个数。
第 2 行 n 个用空格隔开的整数，为每个节点的分数

输出格式
第 1 行 1 个整数，为最高加分（Ans≤4,000,000,000）。
第 2 行 n 个用空格隔开的整数，为该树的前序遍历。

输入输出样例
输入
5
5 7 1 2 10

输出
145
3 1 2 4 5

说明/提示
数据规模与约定
对于全部的测试点，保证 1≤n<30，节点的分数是小于 100 的正整数，答案不超过 4×10^9。*/

#include <stdio.h>

struct TreeNode {
    
};

int main() {
    int n;
    scanf("%d", &n);

}